linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - unused
    - revive
    - gofmt
    - misspell

linters-settings:
  revive:
    rules:
      - name: exported
        disabled: true

run:
  timeout: 5m
